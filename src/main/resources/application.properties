spring.application.name=demo16

# Database Configuration for Railway
spring.datasource.url=${DATABASE_URL:jdbc:mysql://mysql.railway.internal:3306/railway}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:WNxxPSXDmeatoBsxqlWGgaJKIDbISsoe}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Server Configuration
server.port=${PORT:8080}

# Logging Configuration
logging.level.org.springframework.security=INFO
logging.level.com.example.demo16=INFO
logging.level.org.hibernate.SQL=WARN

# Actuator for Health Checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
